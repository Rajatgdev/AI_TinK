{
  "name": "remember-me",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "dev:api": "npm run dev --workspace=@remember-me/api",
    "dev:listener": "npm run dev --workspace=@remember-me/listener",
    "list:chats": "npm run list:chats --workspace=@remember-me/listener"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/pg": "^8.23.1",
    "typescript": "^5.7.3"
  }
}
